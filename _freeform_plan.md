# Re-run the aces MUL-fixture implementation (directive #12832): fixture-backed MUL access for smoke tests plus typed error results replacing the silent empty-list degradation. Previous run died at Phase 1 on an Anthropic 429 session-limit, so this is a straight retry of the same scope.

## Phase 1: Re-run the aces MUL-fixture implementation (directive #12832): fixture-backed MUL access for smoke tests plus typed error results replacing the silent empty-list degradation. Previous run died at Phase 1 on an Anthropic 429 session-limit, so this is a straight retry of the same scope.
Re-dispatch of directive #12832 in the aces project (/home/arcwhite/Documents/aces), worktree d-12832. This is the standalone MUL-fixture task Andy asked for: replace live MUL (Master Unit List) calls in the smoke-test path with fixtures so smoke tests stop hitting the live MUL service, and replace the silent-failure antipattern where every error mode (rate limit, DNS failure, Postgres error, zero-match) collapses to an empty result list — use typed results instead: {:ok, {units, source}} vs {:error, {:mul_unavailable, reason}} vs {:error, {:query_failed, reason}}, naming the cause rather than degrading it.

Prior attempt context (important, do not repeat it): the earlier run failed on Phase 1 after a single attempt. The failure was NOT a code or task problem — the claude subprocess exited 1 immediately after the system/init event with api_error_status 429, "You've hit your session limit · resets 10pm (Australia/Sydney)", total_cost_usd 0, one turn. That is Anthropic session-quota exhaustion, not an implementation failure. So this is a clean retry of the same work, not a rework: start Phase 1 from scratch on a fresh branch/worktree state (verify the branch diff is empty and rebased on aces main before starting — a non-empty stale diff means the previous attempt left residue).

This was a phased/plan-driven execution. Recover the plan path from directive #12832's own record rather than assuming; if the plan is present under data/plans/aces/, run phased execution against it with the usual per-phase test gates and review. Keep phase scope under ~400 LOC. Note quota resets at 10pm Australia/Sydney — if the retry hits 429 again, surface it as a typed quota error to Andy immediately rather than reporting a generic phase failure.
